[package]
name = "compositor_installer_process_layout_compute_retire"
version.workspace = true
edition.workspace = true

[dependencies]
compositor_installer_process_layout_compute_stage = { path = "../compute.stage" }

[lib]
path = "lib.rs"
