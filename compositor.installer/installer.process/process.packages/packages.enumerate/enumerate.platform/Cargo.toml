[package]
name = "compositor_installer_process_packages_enumerate_platform"
version.workspace = true
edition.workspace = true

[dependencies]

[lib]
path = "lib.rs"

[package.metadata.lint]
allow = ["size"]
reason = "Per-distro package enumeration list; inherently a flat list, splitting adds no value."
