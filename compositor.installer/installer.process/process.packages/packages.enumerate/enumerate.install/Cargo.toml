[package]
name = "compositor_installer_process_packages_enumerate_install"
version.workspace = true
edition.workspace = true

[dependencies]
compositor_installer_process_packages_enumerate_platform = { path = "../enumerate.platform" }

[lib]
path = "lib.rs"
