[package]
name = "compositor_support_iced_font_default_base"
version = "0.0.1"
edition = "2024"

[dependencies]
iced_graphics = { workspace = true }
# Developer logging — provides error!/warn!/info!/trace!/abort! (see lib.rs).
compositor_model_debug_instance_record = { workspace = true }

[lib]
path = "lib.rs"
