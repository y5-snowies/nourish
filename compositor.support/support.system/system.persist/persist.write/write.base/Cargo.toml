[package]
name = "compositor_support_system_persist_write_base"
version = "0.0.1"
edition = "2024"

[dependencies]
# Developer logging — provides error!/warn!/info!/trace!/abort! (see lib.rs).
compositor_model_debug_instance_record = { workspace = true }
uuid = { workspace = true }

[lib]
path = "lib.rs"
