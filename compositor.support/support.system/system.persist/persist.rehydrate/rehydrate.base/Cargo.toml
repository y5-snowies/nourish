[package]
name = "compositor_support_system_persist_rehydrate_base"
version = "0.0.1"
edition = "2024"

[dependencies]
compositor_support_system_storage_slot_base = { workspace = true }
compositor_support_system_persist_entry_base = { workspace = true }
compositor_support_system_persist_envelope_base = { workspace = true }
compositor_support_system_persist_path_base = { workspace = true }
# Developer logging — provides error!/warn!/info!/trace!/abort! (see lib.rs).
compositor_model_debug_instance_record = { workspace = true }
uuid = { workspace = true }

[lib]
path = "lib.rs"
