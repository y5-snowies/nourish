[package]
name = "compositor_kernel_graphic_draw_present_callbacks"
version = "0.0.1"
edition = "2024"

[dependencies]
compositor_model_debug_instance_record.workspace = true
bitflags.workspace = true
smithay.workspace = true

# Compositor dependencies (CI ensures availability in workspace)
compositor_orchestration_core_state_base.workspace = true

[lib]
path = "lib.rs"
