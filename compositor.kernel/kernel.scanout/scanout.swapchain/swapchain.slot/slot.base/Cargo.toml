[package]
name = "compositor_kernel_scanout_swapchain_slot_base"
version = "0.0.1"
edition = "2024"

[dependencies]
compositor_model_debug_instance_record.workspace = true
bitflags.workspace = true
smithay.workspace = true

[features]
# The native scanout machine (de-delegation), compile gate.
native-scanout = []

[lib]
path = "lib.rs"
