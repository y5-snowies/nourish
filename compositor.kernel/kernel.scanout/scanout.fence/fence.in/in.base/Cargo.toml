[package]
name = "compositor_kernel_scanout_fence_in_base"
version = "0.0.1"
edition = "2024"

[dependencies]
compositor_model_debug_instance_record.workspace = true
bitflags.workspace = true
smithay.workspace = true

# Compositor dependencies (CI ensures availability in workspace)
compositor_kernel_scanout_commit_build_base.workspace = true

[features]
native-scanout = ["compositor_kernel_scanout_commit_build_base/native-scanout"]

[lib]
path = "lib.rs"
