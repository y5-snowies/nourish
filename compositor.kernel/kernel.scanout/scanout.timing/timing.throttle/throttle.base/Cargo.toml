[package]
name = "compositor_kernel_scanout_timing_throttle_base"
version = "0.0.1"
edition = "2024"

[dependencies]
compositor_model_debug_instance_record.workspace = true
bitflags.workspace = true
smithay.workspace = true

[features]
# Law-7 double gate, compile-time half (early-vblank re-timing).
timing-throttle = []

[lib]
path = "lib.rs"
