[package]
name = "compositor_kernel_native_context_display_apply"
version = "0.0.1"
edition = "2024"

[dependencies]
smithay = { workspace = true }
compositor_kernel_native_context_render_base = { workspace = true }
compositor_kernel_scanout_surface_output_base = { workspace = true }
compositor_orchestration_core_state_base = { workspace = true }
compositor_orchestration_driver_lid_base = { workspace = true }
compositor_orchestration_driver_logind_base = { workspace = true }
# Developer logging — provides error!/warn!/info!/trace!/abort! (see lib.rs).
compositor_model_debug_instance_record = { workspace = true }

[lib]
path = "lib.rs"
