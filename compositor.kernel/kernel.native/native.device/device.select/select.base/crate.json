{
  "deps": [
    "compositor_model_debug_instance_record",
    // Compositor dependencies (CI ensures availability in workspace)
    "compositor_kernel_gpu_preference_select_base",
    "compositor_kernel_graphic_preference_gpu_rank"
  ]
}
