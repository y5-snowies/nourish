[package]
name = "compositor_orchestration_driver_logind_base"
version = "0.0.1"
edition = "2024"

[dependencies]
zbus = { workspace = true }
compositor_support_system_storage_token_base = { workspace = true }
# Developer logging — provides error!/warn!/info!/trace!/abort! (see lib.rs).
compositor_model_debug_instance_record = { workspace = true }

[lib]
path = "lib.rs"
