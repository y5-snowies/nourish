[package]
name = "compositor_orchestration_world_manager_base"
version = "0.0.1"
edition = "2024"

[dependencies]
compositor_support_system_storage_slot_base = { workspace = true }
compositor_support_system_world_host_base = { workspace = true }
compositor_support_system_world_identity_base = { workspace = true }
uuid = { workspace = true }

[lib]
path = "lib.rs"

[package.metadata.lint]
allow = ["size"]
reason = "WorldManager is one cohesive owner of the world set + active/spawn-target/generation invariants."
