[package]
name = "compositor_orchestration_draw_dispatch_uniforms"
version = "0.0.1"
edition = "2024"

[dependencies]
smithay = { workspace = true }
# The bundle's own requirement vocabulary, so a gate in the renderer and the
# manifest entry that authorised it are literally the same type. A serde-only
# leaf crate — it depends on nothing here, so this edge cannot cycle.

[lib]
path = "lib.rs"
