[package]
name = "compositor_monitor_overview_ui_base"
version = "0.0.1"
edition = "2024"

[dependencies]
compositor_support_iced_core_engine_base = { workspace = true }
iced_core = { workspace = true }
iced_widget = { workspace = true }
# Developer logging — provides error!/warn!/info!/trace!/abort! (see lib.rs).
compositor_model_debug_instance_record = { workspace = true }

[lib]
path = "lib.rs"
