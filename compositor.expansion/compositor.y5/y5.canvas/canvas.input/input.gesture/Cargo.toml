[package]
name = "compositor_y5_canvas_input_gesture"
version = "0.0.1"
edition = "2024"

[dependencies]
compositor_orchestration_core_state_base = { workspace = true }
compositor_y5_navigator_interface_base = { workspace = true }
compositor_support_action_camera_find_base = { workspace = true }
# Developer logging — provides error!/warn!/info!/trace!/abort! (see lib.rs).
compositor_model_debug_instance_record = { workspace = true }
compositor_model_environment_config_base = { workspace = true }

[lib]
path = "lib.rs"
