[package]
name = "compositor_y5_graphic_capture_vaapi"
version = "0.0.1"
edition = "2024"
build = "build.rs"

[dependencies]
smithay = { workspace = true }
# Developer logging — provides error!/warn!/info!/trace!/abort! (see lib.rs).
compositor_model_debug_instance_record = { workspace = true }
# Settings: the `capture_encoder` field selects the hardware video backend.
compositor_model_environment_config_base = { workspace = true }

[build-dependencies]
bindgen = { workspace = true }
pkg-config = { workspace = true }
cc = { workspace = true }

[lib]
path = "lib.rs"
