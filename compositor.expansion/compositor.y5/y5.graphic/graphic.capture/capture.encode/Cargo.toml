[package]
name = "compositor_y5_graphic_capture_encode"
version = "0.0.1"
edition = "2024"

[dependencies]
wgpu = { workspace = true }
compositor_support_bevy_core_runtime_base = { workspace = true }
zbus = { workspace = true }
image = { workspace = true }
# Developer logging — provides error!/warn!/info!/trace!/abort! (see lib.rs).
compositor_model_debug_instance_record = { workspace = true }

[lib]
path = "lib.rs"
