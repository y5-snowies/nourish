[package]
name = "compositor_y5_overview_draw_fit"
version = "0.0.1"
edition = "2024"

[dependencies]
smithay = { workspace = true }
compositor_y5_window_draw_element = { workspace = true }
compositor_y5_canvas_draw_element = { workspace = true }
# Developer logging — provides error!/warn!/info!/trace!/abort! (see lib.rs).
compositor_model_debug_instance_record = { workspace = true }

[lib]
path = "lib.rs"
