[package]
name = "compositor_y5_overview_draw_blur"
version = "0.0.1"
edition = "2024"

[dependencies]
smithay = { workspace = true }
compositor_support_bevy_core_alloc_base = { workspace = true }
compositor_y5_graphic_capture_registry = { workspace = true }
# Developer logging — provides error!/warn!/info!/trace!/abort! (see lib.rs).
compositor_model_debug_instance_record = { workspace = true }

[lib]
path = "lib.rs"
