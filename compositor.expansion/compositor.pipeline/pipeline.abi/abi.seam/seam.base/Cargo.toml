[package]
name = "compositor_pipeline_abi_seam_base"
version = "0.0.1"
edition = "2024"

[dependencies]
compositor_orchestration_draw_dispatch_frame = { workspace = true }
compositor_pipeline_bundle_require_base = { workspace = true }
compositor_pipeline_abi_worldset_base = { workspace = true }
# Developer logging — provides error!/warn!/info!/trace!/abort! (see lib.rs).
compositor_model_debug_instance_record = { workspace = true }

[lib]
path = "lib.rs"
