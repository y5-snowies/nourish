[package]
name = "compositor_pipeline_bundle_graph_base"
version = "0.0.1"
edition = "2024"

[dependencies]
compositor_pipeline_bundle_manifest_base = { workspace = true }
# `contained` — the one rule about texture paths, stated where the decoder is so
# validation and loading cannot disagree about what a legal path is.
compositor_pipeline_compile_decode_base = { workspace = true }

[lib]
path = "lib.rs"
